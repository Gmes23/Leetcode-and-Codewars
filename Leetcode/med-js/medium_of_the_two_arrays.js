var findMedianSortedArrays = function(nums1) {
    // % remainer operator 
    // The JavaScript Math object allows you 
    //to perform mathematical tasks on numbers.
    // Return the number of characters in a string:
    // return console.log(Math.floor(nums1.length))
    for (const i in nums1) {
    }
    
};

findMedianSortedArrays([7,2,5])